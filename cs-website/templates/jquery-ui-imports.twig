{% set scripts = (scripts is defined ? scripts : [])|merge([]) %}
{% set styles = (styles is defined ? styles : [])|merge(["lib/jquery-ui.min", "lib/jquery-ui.structure.min", "lib/jquery-ui.theme.min"]) %}